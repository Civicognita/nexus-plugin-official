{
  "mcpServers": {
    "tynn-mcp": {
      "type": "sse",
      "url": "${TYNN_API_URL:-https://app.tynn.ai}/mcp/tynn",
      "headers": {
        "Authorization": "Bearer ${TYNN_API_KEY}"
      }
    }
  }
}

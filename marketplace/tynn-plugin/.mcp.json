{
  "mcpServers": {
    "tynn-mcp": {
      "type": "http",
      "url": "${TYNN_API_URL:-https://tynn.ai}/mcp/tynn",
      "headers": {
        "Authorization": "Bearer ${TYNN_API_KEY}"
      }
    }
  }
}
